DATABASE_URL=postgresql+asyncpg://temus:temus@localhost:5432/temus
ENVIRONMENT=development

# OpenAI — chat, vision, audio (Day 1+)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Redis — cache + background jobs
REDIS_URL=redis://localhost:6379/0

# Langfuse — LLM tracing (self-hosted, http://localhost:3000)
# Get keys after first-run signup at the Langfuse UI; leave blank to disable tracing.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=http://localhost:3000
